streamlit>=1.32.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
langchain>=0.2.11
langchain-community>=0.2.11
langchain-openai>=0.1.0
langchain-qdrant>=0.2.0
langsmith>=0.1.0
qdrant-client>=1.9.0
python-dotenv>=1.0.0
unstructured>=0.12.0
pypdf>=4.0.0
docx2txt>=0.8
chardet>=5.2.0
rank-bm25>=0.2.2

# Docling-based PDF ingestion (layout/table-aware alternative to the
# PyPDFLoader + RecursiveCharacterTextSplitter pipeline in ingest/embed_and_store.py)
docling>=2.0.0

# Auth + chat log persistence
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0

# Trusts the Windows certificate store for outbound HTTPS (fixes SSL verification
# failures to api.openai.com on machines behind a TLS-inspecting corporate proxy)
pip-system-certs>=4.0
